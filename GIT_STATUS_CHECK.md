# 🔍 Проверка статуса Git

## Быстрая проверка

Выполните в терминале:

```bash
cd /Volumes/Data/Development/Dripify

# Проверка статуса
git status

# Или используйте скрипт
./check-git-status.sh
```

## Что проверить:

### 1. Инициализирован ли Git?

```bash
git status
```

**Если видите:** `fatal: not a git repository`  
**Решение:** Выполните `git init`

### 2. Есть ли незакоммиченные файлы?

```bash
git status
```

**Проверьте:**
- `Untracked files` - файлы, которые не добавлены в Git
- `Changes not staged for commit` - измененные файлы, не добавленные в staging
- `Changes to be committed` - файлы в staging, готовые к коммиту

### 3. Все ли файлы закоммичены?

```bash
# Проверка последних коммитов
git log --oneline -5

# Проверка незакоммиченных изменений
git diff --name-only

# Проверка неотслеживаемых файлов
git ls-files --others --exclude-standard
```

## 📋 Чеклист файлов, которые должны быть в Git:

- ✅ `package.json`
- ✅ `package-lock.json`
- ✅ `webpack.config.js`
- ✅ `vercel.json`
- ✅ `README.md`
- ✅ `DEPLOYMENT.md`
- ✅ `QUICK_DEPLOY.md`
- ✅ `NEXT_STEPS.md`
- ✅ `src/` (вся директория)
- ✅ `public/` (вся директория)
- ✅ `.gitignore`
- ❌ `node_modules/` (должно быть в .gitignore)
- ❌ `dist/` (должно быть в .gitignore)

## 🚀 Если нужно добавить все файлы:

```bash
# Добавить все файлы
git add .

# Проверить, что добавлено
git status

# Создать коммит
git commit -m "Update: добавлены все последние изменения"
```

## 📝 Если нужно проверить конкретные файлы:

```bash
# Проверить, отслеживается ли файл
git ls-files | grep "имя_файла"

# Проверить изменения в конкретном файле
git diff src/components/FilterChip.vue

# Проверить историю файла
git log --oneline -- src/components/FilterChip.vue
```





