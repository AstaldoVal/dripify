# 🚀 Настройка автоматического деплоя на Vercel

## 📋 Как это работает

При каждом **push** в репозиторий (GitHub/GitLab/Bitbucket) Vercel автоматически:
1. Клонирует репозиторий
2. Устанавливает зависимости (`npm install`)
3. Собирает проект (`npm run build`)
4. Деплоит на production

## 🔧 Настройка (5 минут)

### Шаг 1: Подключение репозитория к Vercel

#### Вариант A: Через веб-интерфейс (Рекомендуется)

1. **Зайдите на [vercel.com](https://vercel.com)**
2. **Войдите** через GitHub/GitLab/Bitbucket (используйте тот же аккаунт, где находится репозиторий)
3. **Нажмите "Add New Project"** (или **"New Project"**)
4. **Выберите ваш Git провайдер** (GitHub/GitLab/Bitbucket)
5. **Найдите репозиторий `dripify`** в списке
6. **Нажмите "Import"**

#### Вариант B: Через Vercel CLI

```bash
# 1. Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# 2. Войдите в Vercel
vercel login

# 3. Подключите проект к Git репозиторию
cd /Volumes/Data/Development/Dripify
vercel link

# Следуйте инструкциям:
# - Set up and deploy? Y
# - Which scope? (выберите ваш аккаунт)
# - Link to existing project? N (для первого раза)
# - Project name? dripify
# - Directory? ./
```

### Шаг 2: Настройка проекта

Vercel автоматически определит настройки из `vercel.json`, но проверьте:

**Framework Preset:** `Other`  
**Root Directory:** `./`  
**Build Command:** `npm run build`  
**Output Directory:** `dist`  
**Install Command:** `npm install`

### Шаг 3: Настройка веток (опционально)

В настройках проекта можно настроить:

- **Production Branch:** `main` (или `master`)
- **Preview Branches:** все остальные ветки будут деплоиться как preview
- **Auto-deploy:** включено по умолчанию

### Шаг 4: Первый деплой

После подключения репозитория Vercel автоматически:
1. Выполнит первый деплой
2. Создаст production URL (например: `https://dripify.vercel.app`)

## ✅ Проверка автоматического деплоя

### Тест 1: Push в main ветку

```bash
# Сделайте любое изменение
echo "# Test" >> README.md

# Закоммитьте и отправьте
git add README.md
git commit -m "Test: проверка автоматического деплоя"
git push origin main
```

**Результат:** Vercel автоматически запустит новый деплой (проверьте в Vercel Dashboard)

### Тест 2: Создание Pull Request

```bash
# Создайте новую ветку
git checkout -b feature/test-auto-deploy

# Сделайте изменения
echo "# Feature" >> README.md
git add .
git commit -m "Feature: тестовая функция"
git push origin feature/test-auto-deploy
```

**Результат:** Vercel создаст preview deployment для этой ветки

## 📊 Мониторинг деплоев

### В Vercel Dashboard:

1. Зайдите в ваш проект на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Откройте вкладку **"Deployments"**
3. Вы увидите:
   - ✅ Успешные деплои (зеленый)
   - ⏳ Деплои в процессе (желтый)
   - ❌ Неудачные деплои (красный)

### Уведомления:

Vercel автоматически отправляет уведомления:
- 📧 Email при завершении деплоя
- 💬 Комментарии в Pull Request (если включено)
- 🔔 Уведомления в Vercel Dashboard

## 🔧 Дополнительные настройки

### Environment Variables

Если нужны переменные окружения:

1. В Vercel Dashboard → Project → Settings → Environment Variables
2. Добавьте переменные:
   - `NODE_ENV=production`
   - Другие переменные по необходимости
3. Выберите окружения: Production, Preview, Development

### Custom Domain

Для подключения собственного домена:

1. Vercel Dashboard → Project → Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

### Webhooks

Для интеграции с другими сервисами:

1. Vercel Dashboard → Project → Settings → Git
2. Найдите раздел "Deploy Hooks"
3. Создайте webhook для уведомлений

## 🐛 Решение проблем

### Проблема: Деплой не запускается автоматически

**Решение:**
1. Проверьте, что репозиторий подключен к Vercel
2. Убедитесь, что вы делаете push в правильную ветку (обычно `main`)
3. Проверьте настройки Git в Vercel Dashboard → Settings → Git

### Проблема: Build fails

**Решение:**
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что `package.json` содержит все зависимости
3. Проверьте, что `vercel.json` настроен правильно
4. Убедитесь, что Node.js версия совместима

### Проблема: Деплой запускается, но сайт не работает

**Решение:**
1. Проверьте, что `outputDirectory` в `vercel.json` правильный (`dist`)
2. Убедитесь, что в `vercel.json` есть rewrites для SPA
3. Проверьте консоль браузера на ошибки

## 📝 Чеклист настройки

- [ ] Репозиторий создан на GitHub/GitLab/Bitbucket
- [ ] Код отправлен в репозиторий (`git push`)
- [ ] Vercel аккаунт создан
- [ ] Репозиторий подключен к Vercel
- [ ] Настройки проекта проверены
- [ ] Первый деплой успешно завершен
- [ ] Тестовый push запустил автоматический деплой
- [ ] Production URL работает

## 🎯 Best Practices

1. **Используйте ветки:**
   - `main` → Production
   - `develop` → Preview
   - Feature branches → Preview deployments

2. **Проверяйте preview deployments:**
   - Каждый PR получает свой preview URL
   - Тестируйте перед merge в main

3. **Мониторьте деплои:**
   - Проверяйте логи при ошибках
   - Используйте Vercel Analytics для мониторинга

4. **Используйте Environment Variables:**
   - Не храните секреты в коде
   - Используйте разные значения для Production/Preview

## 🔗 Полезные ссылки

- [Vercel Git Integration Docs](https://vercel.com/docs/concepts/git)
- [Vercel Automatic Deployments](https://vercel.com/docs/concepts/deployments/overview)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

**После настройки каждый `git push` будет автоматически деплоить ваш проект! 🚀**





