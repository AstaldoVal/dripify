# Инструкция по релизу и деплою Dripify

## 📋 Подготовка к релизу

### 1. Проверка проекта

Убедитесь, что проект собирается без ошибок:

```bash
npm run build
```

### 2. Проверка зависимостей

Убедитесь, что все зависимости установлены:

```bash
npm install
```

## 🚀 Релиз в Git

### Шаг 1: Инициализация Git репозитория (если еще не сделано)

```bash
# Проверьте, есть ли уже git репозиторий
git status

# Если репозиторий не инициализирован, выполните:
git init
```

### Шаг 2: Добавление всех файлов

```bash
# Добавьте все файлы в staging
git add .

# Проверьте, что будет закоммичено
git status
```

### Шаг 3: Создание первого коммита

```bash
git commit -m "Initial commit: Dripify Lead Management Platform

- Vue 3 SPA with Webpack
- Lead management with AI-powered stages
- Inline filter builder
- Kanban and Table views
- Dark/Light theme support"
```

### Шаг 4: Создание репозитория на GitHub/GitLab

1. Создайте новый репозиторий на GitHub (или GitLab)
2. Скопируйте URL репозитория (например: `https://github.com/username/dripify.git`)

### Шаг 5: Добавление remote и push

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/username/dripify.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код в репозиторий
git push -u origin main
```

### Шаг 6: Создание тега для релиза

```bash
# Создайте тег для версии
git tag -a v1.0.0 -m "Release version 1.0.0"

# Отправьте тег в репозиторий
git push origin v1.0.0
```

## 🌐 Деплой на Vercel

### Вариант 1: Деплой через Vercel CLI

#### Шаг 1: Установка Vercel CLI

```bash
npm install -g vercel
```

#### Шаг 2: Логин в Vercel

```bash
vercel login
```

#### Шаг 3: Деплой проекта

```bash
# Перейдите в корневую директорию проекта
cd /Volumes/Data/Development/Dripify

# Запустите деплой
vercel

# Следуйте инструкциям:
# - Set up and deploy? Y
# - Which scope? (выберите ваш аккаунт)
# - Link to existing project? N (для первого деплоя)
# - Project name? dripify (или другое имя)
# - Directory? ./
# - Override settings? N
```

#### Шаг 4: Production деплой

```bash
# Для production деплоя
vercel --prod
```

### Вариант 2: Деплой через GitHub Integration (Рекомендуется)

#### Шаг 1: Подключение репозитория

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Выберите ваш GitHub аккаунт
4. Найдите репозиторий `dripify` и нажмите "Import"

#### Шаг 2: Настройка проекта

Vercel автоматически определит настройки из `vercel.json`, но проверьте:

- **Framework Preset**: Other
- **Root Directory**: `./`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

#### Шаг 3: Environment Variables (если нужны)

Если у вас есть переменные окружения, добавьте их в разделе "Environment Variables"

#### Шаг 4: Деплой

Нажмите "Deploy" - Vercel автоматически:
1. Склонирует репозиторий
2. Установит зависимости
3. Запустит build
4. Задеплоит проект

### Вариант 3: Деплой через Git Push

После подключения репозитория к Vercel, каждый push в `main` ветку автоматически запускает деплой:

```bash
git push origin main
```

## 🔧 Настройка для Production

### 1. Обновление package.json

Убедитесь, что в `package.json` есть правильные скрипты:

```json
{
  "scripts": {
    "dev": "webpack serve --mode development",
    "build": "webpack --mode production",
    "serve": "webpack serve --mode development"
  }
}
```

### 2. Проверка webpack.config.js

Убедитесь, что webpack настроен для production build с правильным output:

```javascript
output: {
  path: path.resolve(__dirname, 'dist'),
  filename: '[name].[contenthash].js',
  clean: true
}
```

### 3. Оптимизация для Production

- Минификация кода (webpack делает это автоматически в production mode)
- Оптимизация изображений
- Настройка кеширования

## 📝 Чеклист перед релизом

- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Все зависимости установлены
- [ ] `.gitignore` настроен правильно
- [ ] Код закоммичен в git
- [ ] Репозиторий создан на GitHub/GitLab
- [ ] Код отправлен в remote репозиторий
- [ ] Тег версии создан
- [ ] `vercel.json` настроен
- [ ] Проект подключен к Vercel
- [ ] Деплой успешно завершен
- [ ] Сайт доступен по URL от Vercel

## 🐛 Решение проблем

### Проблема: Build fails на Vercel

**Решение:**
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что `package.json` содержит все зависимости
3. Проверьте, что Node.js версия совместима (Vercel использует Node 18.x по умолчанию)

### Проблема: 404 на всех роутах

**Решение:**
Убедитесь, что в `vercel.json` есть rewrites для SPA:

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### Проблема: Статические файлы не загружаются

**Решение:**
Проверьте, что в `webpack.config.js` правильный `publicPath`:

```javascript
output: {
  publicPath: '/'
}
```

## 🔗 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vue.js Deployment Guide](https://vuejs.org/guide/scaling-up/deployment.html)
- [Webpack Production Guide](https://webpack.js.org/guides/production/)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте консоль браузера
3. Убедитесь, что все зависимости установлены

---

**Удачи с деплоем! 🚀**

